# General  # default TrueFlask configuration
SECRET_KEY        = '{{ flask_secret_key }}'
portalTitleText   = '{{ flask_portal_title }}'
logoUrl           = '{{ flask_logo_url }}' 
YODA_VERSION      = '{{ yoda_version }}'
YODA_COMMIT       = '{{ yoda_commit }}'
modules           = [
     {'name': 'Research', 'function': 'research_bp.index'}
    ,{'name': 'Vault', 'function': 'vault_bp.index'}
    ,{'name': 'Statistics', 'function': 'stats_bp.index'}
    ,{'name': 'Group Manager', 'function': 'group_bp.index'}
{% if enable_intake %}
    ,{'name': 'Intake', 'function': 'intake_bp.index'} 
{% endif %}
{% if enable_datarequest %}
    ,{'name': 'Datarequest', 'function': 'datarequest_bp.index'}
{% endif %}
    ]

# Flask-Session configuration
SESSION_TYPE = '{{ flask_session_type }}'
SESSION_COOKIE_HTTPONLY = True
SESSION_COOKIE_SECURE = True 
#PERMANENT_SESSION_LIFETIME = {{ flask_permanent_session_lifetime }} 
#SESSION_PERMANENT = False
SESSION_USE_SIGNER = True                 # Use signer for cookie session sid
SESSION_FILE_DIR = '{{ flask_session_file_dir }}'  # default flask_session under current working dir
