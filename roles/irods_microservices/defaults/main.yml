---
# copyright Utrecht University

rolename: irods-microservices

# iRODS configuration.
irods_service_account: irods

# Temporary directory to store downloaded package files.
irods_microservices_package_dest_dir: /tmp


repo_only: false  # Only download packages from repos (this only applies to CentOS/RHEL)
