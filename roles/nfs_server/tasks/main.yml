---
# copyright Utrecht University

- name: Include OS-specific variables.
  ansible.builtin.include_vars: "{{ ansible_os_family | lower }}.yml"


- name: Ensure setup/install tasks
  ansible.builtin.include_tasks: "{{ ansible_os_family | lower }}.yml"


- name: Extract vault paths for NFS exports, filtering by Vault and unixfilesystem
  ansible.builtin.set_fact:
    nfs_exports: "{{ irods_resources | selectattr('vault_path', 'defined') | selectattr('resource_type', 'equalto', 'unixfilesystem') | selectattr('vault_path', 'search', '^/var/lib/irods/Vault') | map(attribute='vault_path') | list }}"


- name: Ensure NFS export directories exist
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    owner: root
    group: root
    mode: "0755"
  loop: "{{ nfs_exports }}"
  when: nfs_exports | length > 0


- name: Copy exports file.
  ansible.builtin.template:
    src: exports.j2
    dest: /etc/exports
    owner: root
    group: root
    mode: "0644"
  notify: Reload nfs


- name: Ensure NFS is running.
  ansible.builtin.service:
    name: "{{ nfs_server_daemon }}"
    state: started
    enabled: true
  when: nfs_exports | length > 0
