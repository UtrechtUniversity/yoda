---
# copyright Utrecht University

- name: Ensure firewall is installed
  package:
    name: firewalld
    state: present
  when: change_firewall
  notify: Restart firewall


- name: Ensure firewall is running and enabled
  service:
    name: firewalld
    state: started
    enabled: yes
  when: change_firewall
